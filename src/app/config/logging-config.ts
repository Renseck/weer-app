export const LoggingConfig = {
  // Controls whether icon components log their value changes
  iconLogging: false,
  
  // Controls whether temperature calculations are logged
  tempCalculationLogging: false,
  
  // Controls whether API requests and responses are logged
  apiLogging: true,

  // Controls whether retrievals from logs rather than API are logged
  cacheLogging: true
};